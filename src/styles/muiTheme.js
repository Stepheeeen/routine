// src/styles/muiTheme.js
import { createTheme } from '@mui/material/styles';

const muiTheme = createTheme({
  typography: {
    body1: {
      // Define body1 styles here
    },
    // other typography definitions
  },
  palette: {
    primary: {
      main: '#028960',
    },
    // other palette definitions
  },
});

export default muiTheme;
